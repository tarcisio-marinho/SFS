// rotas basicas da aplicação

// GET / 

// GET /:ID -> retorna se o arquivo existe e quantos dias faltam para ele ser deletado

// POST /file + senha -> faz o upload do arquivo para o servidor, o server vai retornar a url gerada para download do arquivo e o tempo restante para o arquivo ser deletado
